class Interact {
    static AddPeekEntryByModel(pModels: number[], pData, pOptions) {
        globalThis.exports['vrp-interact'].AddPeekEntryByModel(pModels, pData, pOptions)
    }

    static AddPeekEntryByPolyTarget(pEvent: string, pData, pOptions) {
        globalThis.exports['vrp-interact'].AddPeekEntryByPolyTarget(pEvent, pData, pOptions)
    }

    static AddPeekEntryByEntityType(pEntity, pData, pOptions) {
        globalThis.exports['vrp-interact'].AddPeekEntryByEntityType(pEntity, pData, pOptions)
    }
}