function SetPedFlag(object, flag, enabled)
    exports['vrp-flags']:SetPedFlag(object, flag, enabled)
end

function HasPedFlag(object, flag)
    return exports['vrp-flags']:HasPedFlag(object, flag)
end

function GetPedFlags(object)
    return exports['vrp-flags']:GetPedFlags(object)
end

function SetPedFlags(object, flag)
    exports['vrp-flags']:SetPedFlags(object, flag)
end

function SetVehicleFlag(vehicle, flag, enabled)
    exports['vrp-flags']:SetVehicleFlag(vehicle, flag, enabled)
end

function HasVehicleFlag(vehicle, flag)
    return exports['vrp-flags']:HasVehicleFlag(vehicle, flag)
end

function GetVehicleFlags(vehicle)
    return exports['vrp-flags']:GetVehicleFlags(vehicle)
end

function SetVehicleFlags(object, flag)
    exports['vrp-flags']:SetVehicleFlags(object, flag)
end

function SetObjectFlag(object, flag, enabled)
    exports['vrp-flags']:SetObjectFlag(object, flag, enabled)
end

function HasObjectFlag(object, flag)
    return exports['vrp-flags']:HasObjectFlag(object, flag)
end

function GetObjectFlags(object)
    return exports['vrp-flags']:GetObjectFlags(object)
end

function SetObjectFlags(object, flag)
    exports['vrp-flags']:SetObjectFlags(object, flag)
end